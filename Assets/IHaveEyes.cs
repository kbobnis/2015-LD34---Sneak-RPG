using UnityEngine;
using System.Collections;

public class IHaveEyes : MonoBehaviour {

	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
		//Debug.DrawRay(transform.position + transform.up, transform.forward * 10 - transform.up * 1.3f);
		
		//Debug.dra
	}
}
